const prompts=["Clean this project folder, but do not lose anything.","Open the browser and check what broke on this page.","Read the repo, run the check, and bring back the failure.","Prepare a safe edit, backup first, then wait for approval."];const cards=[...document.querySelectorAll('.card')],promptText=document.querySelector('#promptText');let i=0;setInterval(()=>{cards.forEach(c=>c.classList.remove('active'));cards[i%cards.length]?.classList.add('active');if(promptText&&i%cards.length===0)promptText.textContent=prompts[Math.floor(i/cards.length)%prompts.length];i++},1600);document.querySelector('.copy')?.addEventListener('click',async e=>{const b=e.currentTarget,v=b.dataset.copy||'';try{await navigator.clipboard.writeText(v);b.textContent='Copied';setTimeout(()=>b.textContent='Copy link',1500)}catch{b.textContent=v}});